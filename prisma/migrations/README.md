# Migrări Prisma

Migrările se generează automat cu:

```bash
npm run db:migrate
```

La deployment în producție, folosiți:

```bash
npm run db:migrate:deploy
```

**Nu editați manual fișierele din acest director.**
